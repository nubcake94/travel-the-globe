import 'package:flutter/material.dart';

class AppColorPalette {
  AppColorPalette._();

  //Websafe colors used
  static const Ceil = Color(0xFF9999CC);
  static const DeepKoamaru = Color(0xFF333366);
}
